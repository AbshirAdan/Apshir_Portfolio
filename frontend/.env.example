# Frontend environment variables (copy to .env)

# API base URL — use /api in development (Vite proxy) or full URL in production
VITE_API_URL=/api
#https://abshir-portfolio-api.onrender.com/api

# Optional: public site URL for SEO (production)
# VITE_SITE_URL=https://abshir-portfolio-api.onrender.com/api
